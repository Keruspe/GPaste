3.0:
    libgpaste:
        simplify gpaste-settings ? (macros ?)
        remove useless targets from clipboard callbacks
    libgpaste-client:
        pkg-config
        specify errors types when possible in g-i
        refactor common dbus stuff
    gpaste:
        rewrite in C
    gnome-shell:
        prefs.js

future:
    libgpaste:
        handle keyboard layout changes
