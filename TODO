3.0:
    libgpaste-client:
        NameLost
        Tracking
        ReexecuteSelf
        Active
    libgpaste-keybinder:
        fix paste & pop
    gnome-shell:
        port to libgpaste-client
        prefs.js

future:
    gpaste:
        GSubProcess
    libgpaste:
        handle keyboard layout changes
    libgpaste-client:
        specify errors types when possible in g-i
