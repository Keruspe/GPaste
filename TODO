3.0:
    all:
        includable headers
        pkg-config split
    settings:
        close button
        conditional gnome-shell stuff

future:
    gpaste:
        GSubProcess
    libgpaste-client:
        specify errors types when possible in g-i
