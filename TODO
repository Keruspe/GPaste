3.16:
    general:
        fix histories view in settings refresh
        confirm dialog for history deletion
        FIXMEs
    applet:
        menu for app-indicator and applet right click
            - ui
            - settings
            - quit
    gpaste-ui:
        add appstream

3.18:
    general:
        dbus errors instead of asserts in daemon
    gpaste-ui:
        search bar
        history chooser

future:
    general:
        wayland support
        add class methods for signals
        gpg support
        properties all the way (when GProperty stuff lands)
    libgpaste-client:
        specify errors types when possible in g-i
    tests:
        split test, add interactive wrapper

upstream:
    const in *_get_private
    const in G_DECLARE macros
