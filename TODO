2.6:
    multiple histories
    FILO/LIFO
    show-history needs left-right before navigating

3.0:
    prefs.js
    handle keyboard layout changes
    libgpaste-client + introspection/vapi
    rewrite gpaste in C

future:
    build: depend on intltool 0.50 to remove _NOMERGE hack
    build: depend on >automake 1.11.5 to avoid shipping vapi and fix vapi hack
