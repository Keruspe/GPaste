3.0:
    all:
        guard multi dispose calls
        don't static c_signals
    settings:
        images-support

future:
    gpaste:
        GSubProcess
    libgpaste-client:
        specify errors types when possible in g-i
