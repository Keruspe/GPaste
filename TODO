3.0:
    libgpaste-client:
        fix Active
    libgpaste-keybinder:
        fix paste & pop
    gnome-shell:
        prefs.js
    all:
        check if all connected signals are tracked

future:
    gpaste:
        GSubProcess
    libgpaste-client:
        specify errors types when possible in g-i
    libgpaste-keybinder:
        disconnect-signal
