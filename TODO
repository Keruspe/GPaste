3.0:
    libgpaste-client:
        Active
    libgpaste-keybinder:
        fix paste & pop
    gnome-shell:
        port to libgpaste-client
        prefs.js

future:
    gpaste:
        GSubProcess
    libgpaste-client:
        specify errors types when possible in g-i
    libgpaste-keybinder:
        disconnect-signal
