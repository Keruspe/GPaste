3.10.2:
    general:
        password support (+ patch password-store)
        https://wiki.gnome.org/HowDoI/DBusApplicationLaunching
        kdbus support (busname)
        gpg support

3.12:
    general:
        merge 3.10.1

3.12.1:
    general:
        merge 3.10.2

3.14:
    general:
        properties all the way
        wayland support

future:
    gnome-shell:
        Try https://github.com/awamper/gpaste-integration
    libgpaste-client:
        specify errors types when possible in g-i
    tests:
        split test, add interactive wrapper

maybe:
    plugins:
        ClipperSync (https://clippersync.com/)
