3.0:
    internal:
        libgpaste-common
    libgpaste:
        simplify gpaste-settings ? (macros ?)
        split off libgpaste-settings
    libgpaste-client:
        Tracking
        ReexecuteSelf
        OnExtensionStateChanged
        Active
    libgpaste-daemon:
        polish and make public
    gpaste:
        defer file reading to daemon
    gnome-shell:
        prefs.js

future:
    libgpaste:
        handle keyboard layout changes
    libgpaste-client:
        specify errors types when possible in g-i
