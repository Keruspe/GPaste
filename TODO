3.7:
    keybinder:
        fix ungrab?
        fix regrab (ptiv->active failed)
    gnome-shell-client:
        async ctor
        add guards
        G_PASTE_MAKE_KEYBINDING()
    gpaste-settings:
        enable/disable extension
    tests:
        split test, add interactive wrapper
    all:
        check if there are missing allow-none
        g_clear_* doesn't need if
        check if we use cleanup for all GError
        review GVariant stuff

3.8:
    unity support

4.0:
    wayland support

future:
    cleanup:
        g_signal_handler_disconnect
        gtk_target_table_free
    gpaste:
        GSubProcess
    libgpaste-client:
        specify errors types when possible in g-i
    ClipperSync support? (https://clippersync.com/)
