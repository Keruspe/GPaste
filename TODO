3.0:
    libgpaste-keybinder:
        fix paste & pop
    gnome-shell:
        prefs.js

future:
    gpaste:
        GSubProcess
    libgpaste-client:
        specify errors types when possible in g-i
