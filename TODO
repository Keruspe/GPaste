3.0:
    libgpaste-client:
        Active
        fix Tracking data
        for OnExtensionStateChanged (timeout)
    libgpaste-daemon:
        fix file reading
    libgpaste-keybinder:
        fix paste & pop
    gnome-shell:
        port to libgpaste-client
        prefs.js
    all:
        check if all connected signals are tracked

future:
    gpaste:
        GSubProcess
    libgpaste-client:
        specify errors types when possible in g-i
    libgpaste-keybinder:
        disconnect-signal
