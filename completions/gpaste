_gpaste()
{
    local cur opts
    COMPREPLY=()
    cur="${COMP_WORDS[${COMP_CWORD}]}"
    opts="add set delete empty start quit applet preferences settings version help -h --help"
    COMPREPLY=( $(compgen -W "$opts" -- $cur ) )
}

complete -F _gpaste gpaste
